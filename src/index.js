import React from 'react'
import ReactDOM from 'react-dom'

const App = () => (
  <div className="app">
    <header className="header">
      <h2 className="title">App Page</h2>
    </header>
  </div>
)

ReactDOM.render(<App />, document.getElementById('root'))